name: dotfiles
root: ~/.config
attach: false
windows:
  - config:
      layout: main-horizontal
      panes:
        - source-code:
            - echo "lets config"
  - nvim:
      layout: main-horizontal
      panes:
        - source-code:
            - cd nvim
