name: dotfiles
root: ~/.config
attach: false
windows:
  - config:
      layout: main-horizontal
      panes:
        - source-code:
            - nvim
        - terminal:
            - echo "hi"
  - nvim:
      layout: main-horizontal
      panes:
        - source-code:
            - cd nvim
            - nvim
        - terminal:
            - cd nvim
            - echo "hi"
