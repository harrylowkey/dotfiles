name: hodfords
root: ~/Dev/hodfords
attach: false
windows:
  - monitors:
      layout: main-horizontal
      panes:
        - monitors:
            - k9s
  - user:
      layout: main-horizontal
      panes:
        - source-code:
            - cd sh-user
        - log:
            - cd sh-user
  - payment:
      layout: main-horizontal
      panes:
        - source-code:
            - cd sh-payment
        - log:
            - cd sh-payment
  - socket:
      layout: main-horizontal
      panes:
        - source-code:
            - cd sh-socket
        - log:
            - cd sh-socket
