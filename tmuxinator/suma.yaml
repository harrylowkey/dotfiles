name: suma
root: ~/Dev/suma
attach: false
windows:
  - backend:
      layout: main-horizontal
      panes:
        - source-code:
            - cd suma-platform-backend
        - terminal:
            - cd suma-platform-backend
  - frontend:
      layout: main-horizontal
      panes:
        - source-code:
            - cd suma-platform-frontend
        - terminal:
            - cd suma-platform-frontend
